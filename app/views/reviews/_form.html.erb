<%= simple_form_for [restaurant, review] do |form| %>

  <%= form.input :content %>
  <%= form.input :rating, collection: (1..5)%>
  <%= form.submit nil, {class:"btn btn-primary mb-1 my-3 w-100"}%>
<% end %>
